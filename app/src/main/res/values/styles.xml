<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="MusicPlayerTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/musicPlayerPrimary</item>
        <item name="colorPrimaryDark">@color/musicPlayerPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="MyFavouriteMusicListTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/myFavouriteMusicListPrimary</item>
        <item name="colorPrimaryDark">@color/myFavouriteMusicListPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <!--弹窗进入、退出动画-->
    <style name="BottomDialogAnimation">
        <item name="android:windowEnterAnimation">@anim/dialog_enter</item>
        <item name="android:windowExitAnimation">@anim/dialog_exit</item>
    </style>

    <!--底部弹窗样式-->
    <style name="BottomAlertDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>  <!--移除边框-->
        <item name="android:windowNoTitle">true</item><!--去除顶部标题栏-->
        <item name="android:windowIsTranslucent">true</item><!--窗体透明-->
        <item name="android:background">@color/colorBottomMusicList</item><!--背景透明-->
        <item name="android:windowBackground">@android:color/transparent</item><!--窗体背景透明-->
        <item name="android:windowIsFloating">false</item><!--窗体是否浮动-->
        <item name="android:backgroundDimEnabled">true</item><!--背景是否昏暗-->
        <item name="android:backgroundDimAmount">0.6</item><!--昏暗数量-->
    </style>

    <!--个人设置-->
    <style name="AnimationLeftFade">
        <item name="android:windowEnterAnimation">@anim/in_lefttoright</item>
        <item name="android:windowExitAnimation">@anim/out_righttoleft</item>
    </style>

</resources>
